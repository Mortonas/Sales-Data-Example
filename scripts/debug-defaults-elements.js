import { defaults } from 'chart.js';

console.log('defaults.elements:', defaults.elements);
if (defaults.elements) {
    console.log('defaults.elements.line:', defaults.elements.line);
    console.log('defaults.elements.point:', defaults.elements.point);
}
